# Infrastructure Terraform Export

Generated by the Architecture Model Service Terraform export feature.

## Context

- Provider: `GCP`
- Environment: `prod`
- Cloud Account: `acme-prod-gcp`
- Location: `europe-west1`
- Generated: <NORMALISED>

## Files

- `main.tf` -- resource blocks per Infrastructure concept.
- `variables.tf` -- variables declared from `terraform_variable_hints` plus standard env/region/project variables.
- `outputs.tf` -- exported outputs.
- `README.md` -- this file.
- `warnings.json` -- soft-warn entries (TODOs surfaced inline as `# TODO` comments).

## Warnings / TODOs

- # TODO: node-pool fields require completion. Source: Compute Cluster 'orders-gke' (id: cl-1).
- # TODO: missing container image (no application_compute_deployments match). Source: Compute Resource 'orders-cr' (id: cr-1).
- # TODO: backend resolution incomplete (no Cloud Run target detected; review load_balancer_resource_routes). Source: Load Balancer 'orders-lb' (id: lb-1).

## Draft Notice

This export is a DRAFT. Review every `# TODO` comment in the generated
files and the `warnings.json` payload before running `terraform plan`.
This export feature does NOT call `terraform init` / `plan` / `apply`,
does NOT contact GCP APIs, and does NOT handle credentials.
