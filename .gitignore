# dependencies
node_modules/

# build output
dist/
build/

# tool output (change dir to outside codebase)
oas-specs/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor
.vscode/
.idea/
.DS_Store

#AI tools
# Anchored to the repo root only so it does NOT clobber
# implement-verify-service/.claude/ (which is intentionally tracked).
/.claude/

HELP.md
target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

#Others
nul
application-local.yml
application-server.yml
*.zip

# V3 evaluation harness reports (human-generated summary output)
discovery-service/evaluation/reports/

# Discovery performance scoring lockfile (perf/PROCESS.md, 2026-04-25)
# All other content under discovery-service/perf/ IS git-tracked.
discovery-service/perf/.scoresheet.lock

# Scratch candidate-export JSON files used during ad-hoc analysis (2026-04-25).
# These are run-specific dumps from `curl …/runs/<id>/candidates`; they're
# useful during investigation but shouldn't end up in git.
discovery-service/openmrs-cands*.json
discovery-service/openmrs-config*.json

# Session / debug artefacts (API dumps saved by curl, scratch scripts, env)
.tmp_*
scratch-*
.env

# Haikai trace logging — host trace dir created by the docker-compose mount
/.haikai-trace/
.haikai/

# AMVS S0 snapshots hold dumped source-DB rows (real business data) -- never committable.
api-migration-validation-service/s0-snapshots/
