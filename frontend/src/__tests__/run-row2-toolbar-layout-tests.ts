/**
 * Test Runner for Row 2 Toolbar Layout Tests
 *
 * Run this file to execute all Row 2 styling controls toolbar tests.
 * Task Group 2 of the Diagram View UI Layout Reorganisation spec.
 *
 * Usage:
 *   npx ts-node src/__tests__/run-row2-toolbar-layout-tests.ts
 */

import { runAllTests } from './row2-toolbar-layout.test';

// Run all Row 2 toolbar layout tests
runAllTests();
