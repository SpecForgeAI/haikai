# Environment and secrets
.env
.env.*
!.env.example
!.env.api.example
!.env.docker

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
# the Python build-dir rule above must not swallow the diagrams client's
# source folder (it silently dropped client/src/lib/utils.ts — every ui/*
# component imports it, so a fresh clone couldn't boot the viewer)
!diagrams/client/src/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv-local/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node.js (local containerized environment)
node_modules/
package-lock.json

# Claude local config
.claude-local/
.claude/scheduled_tasks.lock

# Output and temporary files
output/
*-standards/
staging/
temp/
tmp/
*.log
api_workspace/
*.db

# OS
.DS_Store
Thumbs.db
nul
.venv/

# Test artifacts
tests/integration/test-results/
*.zip
.specforge/
*.struct
*.bak

# autoresearch:debug working dir
debug/
.env.session

# Generated AST analysis demo output (regenerable via `analyze`)
ast-reports/

# haikai / autoresearch run artifacts — now anchored under .haikai/ (gitignored).
# Legacy loose run-dirs (pre-.haikai convention) anchored at repo root so they
# stop being tracked and can't be re-committed.
.haikai/
/predict/
/plan/
/grill/
/loop/
/fix/
/reason/
/learn/
/scenario/
/security/
/ship/
/audit/
/timeline/
/memex/

# Generated coverage / e2e artifacts (regenerated by the test suite)
/.coverage
/e2e_results.json
# Manual SSE / orchestration test scratch at the repo root
/.run*
/.test*
/.shape-spec*
/.orchestrate*
/.standards.response.json
/.edges_def

# haibox remote-ssh runtime ledger (scratch)
/wr/
