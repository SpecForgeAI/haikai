# Critical .gitignore patterns enforced by Haikai prepare_for_commit().
# Language-agnostic — covers common frameworks without being project-specific.
# These are appended to .gitignore if missing, never removed.

haikai_internal:
  # ivs's own per-repo working files — must never be committed into the user's
  # repo. .haikai/config.json in particular was getting swept in by `git add -A`
  # then lost from the working tree on the next branch switch, breaking every
  # subsequent orchestrate / repair with "Project config not found".
  - .haikai/
  - .conversation_history.json
  - chat_logs/

dependencies:
  - node_modules/
  - vendor/
  - __pycache__/
  - "*.pyc"
  - .venv/
  - venv/
  - target/
  - .cargo/

build_output:
  - dist/
  - build/
  - .next/
  - out/
  - coverage/

environment_and_secrets:
  - .env
  - ".env.*"
  - "!.env.example"
  - "*.pem"
  - "*.key"

ide_and_os:
  - .DS_Store
  - Thumbs.db
  - .idea/
  - .vscode/
  - "*.swp"
  - "*.swo"

logs:
  - "*.log"
  - "npm-debug.log*"

lock_files:
  - "*.lock"
  - "!package-lock.json"
  - "!yarn.lock"
  - "!pnpm-lock.yaml"
  - "!Gemfile.lock"
  - "!Cargo.lock"
