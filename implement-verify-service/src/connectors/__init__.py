"""External-system management connectors (control plane).

Distinct from `src/verification/connectors/` — those are narrow CI *verdict*
connectors (trigger/poll a pipeline by SHA for the verification gate). The
modules here are general discover-and-control clients for the same providers.
"""
