# Spec Initialization

This spec's raw idea lives at the spec root: `../raw-idea.md` (read that first — it is
rich and captures program context + locked cross-cutting decisions + open questions).

Spec 1 of the discovery-review-unification program (F → 0 → 1 → 2 → 3). Specs F and 0
are BUILT + verified. This spec builds the DETERMINISTIC review model + cascade/
dependency graph + aggregation backbone that BOTH the candidate grid AND the future
conversational Architect persona (Spec 3) consume. No LLM. No bulk-action execution
(Spec 2). No conversation (Spec 3).

See `requirements.md` (this folder) for the gathered requirements once research is
complete.
