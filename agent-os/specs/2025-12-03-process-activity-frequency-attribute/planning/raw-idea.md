# Raw Idea: Add "Frequency" enum attribute to Process Activities and expose in Activity table

## Feature Description

Title: Add "Frequency" enum attribute to Process Activities and expose in Activity table

Summary:
Extend the Process Activity meta-model to include a new enum attribute `Frequency`. Update the underlying JSON/data model and all relevant UI so that the Activity table displays this new field as a dedicated column, positioned between the existing columns `Description` and `Sequence Order`.

## User Request

The spec folder should be created at: agent-os/specs/2025-12-03-process-activity-frequency-attribute/

Create the planning folder and save the raw idea to planning/raw-idea.md with the full user description.
