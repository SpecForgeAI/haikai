# Dashboard Increment 4 — Add Scope Selector UI + Backend Mock Branching (Mock Data Only)

## Title
Dashboard Increment 4 — Add Scope Selector UI + Backend Mock Branching (Mock Data Only)

## Description/Intent
Add a scope selector to the Dashboard that controls the metrics displayed in the Detailed Definition & Delivery section (pre-coding + post-coding cards). The selector triggers a refetch of the dashboard summary from the existing mock backend endpoint. Backend returns different mock variants per scope. No LLM insights and no real data.

## Key Points
- Add scope selector to Dashboard UI
- Controls metrics displayed in Detailed Definition & Delivery section
- Affects both pre-coding and post-coding cards
- Triggers refetch of dashboard summary from existing mock backend endpoint
- Backend returns different mock variants per scope
- **No LLM insights**
- **No real data** (mock data only)
