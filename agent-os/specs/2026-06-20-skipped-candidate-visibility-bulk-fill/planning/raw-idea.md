TITLE: Skipped-candidate visibility + grouped bulk-fill (C1) for discovery save-back, with duplicate-vs-pre-existing labelling.

PROBLEM: On the current-state architecture Discovery "Candidates" tab, "Save Remaining Approved" / "Save All Approved" reports a single opaque chip e.g. "Saved: 583 created, 148 skipped, 697 committed". The `entitiesSkipped` integer collapses ~25 distinct branches in `mcp-server/src/services/candidateSaveBackService.ts`, and the per-candidate reasons are computed but only `console.warn`'d (never returned, never persisted, never shown). Two consequences: (1) the user has no idea WHAT was skipped or WHY; (2) candidates skipped for missing data stay `approved`/un-committed and re-skip identically on the next Save. On a BRAND-NEW architecture the math (committed 697 = created 583 + reused 114; skipped 148 = reused 114 + genuine 34) shows ~114 of the 148 are INTRA-SCAN DUPLICATES (a second candidate in the SAME save matching an entity the first candidate just created — counted as both skipped AND committed), and only ~34 genuinely blocked. The user wants (a) to be told honestly why things were skipped, and (b) to fix the missing-data ones IN BULK.

DECISIONS ALREADY MADE (FIXED — do NOT relitigate):

1. HONEST SKIP BREAKDOWN (replaces the single integer). The save-result chip must break "skipped" down by reason CLASS, and the chip's reason tokens are clickable to open the remediation panel. Classes:
   - created (newly minted);
   - ALREADY-THERE sub-classes that must be DISTINGUISHED (the explicit "duplicate-vs-pre-existing labelling" ask): (i) INTRA-SCAN DUPLICATE = another candidate in THIS save created the same name+parent first; (ii) PRE-EXISTING = matched an entity committed BEFORE this scan (only possible on re-scans); (iii) ALREADY-SAVED = this exact candidate was committed in a PRIOR save run (filtered pre-loop, not currently counted in "skipped"); plus surface the existing suppressedDuplicates[] (exact-name auto-suppressed) and possibleDuplicates[] (fuzzy-name) the frontend currently drops;
   - BLOCKED = genuine failures that did NOT commit (orphan child/no parent; relationship/interface-link/data-effect with a missing or unresolved source/target reference; dangling-FK prune; enrich/link target gone);
   - QUALITY GAP = committed but missing an important field (e.g. interface with no real interface_type, endpoint with empty operation_verb/path, logical entity with no attributes). These commit today (interface_type defaults to REST_API; verb/path are nullable) but the user wants to fill them.

2. C1 — GROUP-BY-MISSING-FIELD BULK REMEDIATION PANEL (the chosen UX shape; C2 editable-grid/fill-down + C3 inline-row are explicitly OUT/fast-follow). A drawer/panel, opened from the breakdown chip, that GROUPS affected candidates by the SPECIFIC missing/blocking field. Per group: set ONE value (with per-row override) using the right control — a typeahead for reference/FK fields (e.g. the endpoint's interface, a relationship's target entity — autocomplete against existing model entities / approved candidates), a dropdown for enums (e.g. interface_type), free-text otherwise — then PREVIEW (what will now commit / be filled) then COMMIT in ONE transaction. Targets BOTH blockers (unresolved references) AND quality gaps (empty important fields). After committing the fixes, the user re-runs "Save Remaining Approved" (or a "Fix & Save" affordance applies the edits and immediately re-attempts the commit for just those rows). Behavioral precedent to mirror: `MigrationDeliveryBulkResolveModal.tsx` (row drafts -> validate -> preview -> commit).

3. PLACEMENT: inside the Discovery run's Candidates tab (route `/projects/:p/architectures/:a/discovery/runs/:runId?tab=candidates`), NOT inside the architecture-model grids.

BACKEND PREREQUISITES (IN SCOPE):
a. Per-candidate REASON ARM on the save-back result (`SaveBackResult` in `candidateSaveBackService.ts`): at every skip/reuse site (info already in hand — the candidate + a warn message), collect `{ candidateId, candidateType, name, class, reusedSubclass?, missingField?, reason }`; classify reused into intra-scan/pre-existing/already-saved; include the already-existing suppressedDuplicates[]/possibleDuplicates[] arrays. The frontend currently ignores these wire fields. (Decide during shaping: ephemeral-on-result vs also persist a skip_reason on the candidate / emit a linked Finding — Findings already exist as the durable "what's wrong" record.)
b. A NEW bulk-candidate-EDIT endpoint, mirroring the atomic `bulkReviewCascade` (curated id set + field patches, one `@Transactional`), writing the candidate `data` blob / top-level fields via the existing `DiscoveryCandidateService.updateCandidate` logic. (No bulk-edit endpoint exists today; bulk endpoints only set review_status.)
c. A FRONTEND client wrapper over the existing AMS `PUT /{candidateId}` (server already supports full field edits; the UI never wired it — `discoveryApi.ts` only has review/resolve-conflict/bulk-review/save-approved).

GROUNDED REUSE TARGETS (verified by investigation):
- mcp-server `src/services/candidateSaveBackService.ts` (the ~25 skip/reuse branches incl. the reuse match `model.find(e=>e.name===candidate.name && sameParent)` which accumulates in-save creations -> intra-scan dup; SaveBackResult interface ~176-221; suppressedDuplicates/possibleDuplicates/belowGate arrays; per-type required-field logic in convertCandidateToEntity; dedup key = normalized name for top-level, name+parentFk for children); `routes/saveApprovedCandidatesRoute.ts`; gateway `routes/discovery.ts` save-approved passthrough.
- AMS `DiscoveryCandidateService.updateCandidate` (PUT /{candidateId} — edits name/type/confidence/status/operation/data-blob/review fields), `resolveConflict` (single-attribute data write precedent), `DiscoveryCandidateController.bulkReviewCascade` (atomic bulk precedent + BulkReviewCascadeRequest shape), `DiscoveryCandidateEntity` (no skip_reason field today; `data` JSONB payload; review_status proposed/approved/committed/rejected/deferred), `DiscoveryFindingEntity` + `discovery_finding_links` (target_type='discovery_candidate'; evidence_gap gapTypes like interface_missing_contract_detail / data_entity_missing_attributes / endpoint_missing_response_schema, candidate_conflict, ambiguous_relationship — reusable "what's wrong" signal).
- Frontend: `DiscoveryRunDetailPage.tsx` (save result string ~504, chip ~664, owns save-back), `Discovery/DiscoveryRunDetailView.tsx` (tab strip), `DashboardView/DiscoveryCandidateTable.tsx` (table; bulk-action wiring handleOpenCascade ~863 / handleConfirmCascade ~909; filters + confidence slider; NO per-row checkboxes today), `DashboardView/CandidateDetailsPanel.tsx` (read-only detail), `discoveryApi.ts` (`saveApprovedCandidates`, `bulkReviewCascade`, `resolveDiscoveryConflict`, `reviewCandidate`, `getReviewModel`). C1 UI precedents: `ProductManager/MigrationDeliveryDashboard/MigrationDeliveryBulkResolveModal.tsx` (drafts/validate/preview/commit), `Discovery/BulkCandidateActionConfirmModal.tsx` (list-with-reason layout), `Grid/TypeaheadCell.tsx` + `Grid/FreeTextTypeaheadSingleToken.tsx` (FK/typeahead cells — reuse the CELLS, not the Grid container which is bound to the committed model).

OPEN QUESTIONS for the shaper to raise: the exact reason-class taxonomy + user-facing labels; which quality-gap fields are worth surfacing (vs noise); whether a fix that needs a referenced entity that doesn't exist yet should let the user APPROVE a sibling candidate / CREATE the entity inline vs only pick from existing; whether bulk-fill auto-re-commits ("Fix & Save") or requires a manual re-Save; the selection model for the panel (all/filtered/grouped vs new per-row checkboxes); ephemeral reasons vs persisted skip_reason/Finding; how preview computes "will now commit".

NON-GOALS: C2 editable-grid/fill-down and C3 inline-row fix (fast-follow); surfacing uncommitted candidates inside the architecture-model grids; fixing the upstream merge/dedup so fewer intra-scan duplicates reach save-back (separate quality issue — just flag it); changing the commit/save-back algorithm beyond adding the reason arm; multi-role/permissions.
