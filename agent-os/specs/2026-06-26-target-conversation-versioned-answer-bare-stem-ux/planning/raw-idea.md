Feature: Target-state conversation — complete the FR5 framework/version decoupling. This is Spec 1 of a 3-spec initiative and is a PREREQUISITE to Spec 2 (manifest auto-answer) and Spec 3 (decisions-file import), because it defines the answer/template FORMAT both consume.

WHAT EXISTS (do not rebuild): Spec 2026-06-24-target-conversation-tech-stack-constraints (FR5/FR6/FR8) shipped a decoupled framework+version control, but with version-LADEN chips, so it's only half-done:
- frontend `VersionedAnswerControl.tsx` — framework single-select chips + a separate version control (free-text + datalist typeahead, recommended default pre-selected, non-blocking enrichment seam, Spec-4 nudge slot); emits `{framework, version}`; renders one resolved chip; today a two-step "Save version" flow.
- frontend `versionControlConfig.ts` — `VERSIONED_DECISION_CODES` (the 7: service.language, service.framework, service.runtime, db.engine, db.driver, ui.framework, build.tool); `RECOMMENDED_VERSION_BY_FRAMEWORK` keyed by LADEN choice strings (e.g. 'Spring Boot 3.4'); `isVersionedCode`; `buildFrameworkVersionCaptureValue`.
- gateway `frameworkVersionShape.ts` — `{framework, version}` shape, `resolveFrameworkVersionChip` (`${framework} ${version}`), `buildFrameworkVersionEnvelope`; capture rides the existing envelope `answerValue = JSON.stringify({value, sourceQuote, sourceFile})`, `answerSummary` = resolved chip.
- gateway `questionLibrary.ts` — 51 questions; each has `choices`, `versioned`, `cascades[].valueByTriggerValue` (keyed by LADEN strings), `foundationalInputs`, `dependencyClass`.
- Wired in `ConversationMainPane.tsx`: versioned codes render `<VersionedAnswerControl frameworkChoices={choices} />` (question choices verbatim as chips).

THE GAPS THIS SPEC CLOSES (all agreed with the user):
1. BARE-STEM CHIPS: strip the framework axis to bare stems ('[Spring Boot]', '[Java]'), deduping where choices split by version ('Java 21' + 'Java 17' → one '[Java]' chip + a version field). Re-key the recommended-default map to stems. (Today '[Spring Boot 3.4]' + version 4.0 yields a contradictory 'Spring Boot 3.4 4.0'.)
2. DEFAULT VERSION PER STEM: keep existing curated defaults (re-keyed to stems) but SET SPRING BOOT'S DEFAULT TO '4.0' (was 3.4.1). User said "implying nothing else" — do not change the granularity philosophy globally.
3. EXTEND THE VERSION AXIS beyond the 7 codes to all concrete-versioned-library codes. Proposed flip set (to confirm): db.migrations, db.connectionPool, validation.framework, domain.mappingStrategy, logging.framework, metrics.framework, tracing.framework, ui.buildTool, ui.stateManagement, ui.designSystem, testing.unit, testing.integration, testing.e2e, testing.contractTesting, testing.mocking, interservice.asyncBus. Concept/strategy/format codes stay plain chips. Mixed codes: a 'none'/'manual'/'in-house' stem has no version field.
4. COMPACT HORIZONTAL LAYOUT: framework chips left; version to the right as a confirmed chip + "Edit version" button — not a tall vertical box. Drop the italic static context lead-in (repeats the question).
5. AUTO-SELECT VERSION TOGGLE at the top of the conversation, DEFAULT ON: clicking a framework chip immediately COMMITS language + version (default) — one action; change language via another chip; change version via "Edit version" → small text field + "Save version". Toggle OFF = editable field + "Save version" from the start (today's behaviour).
6. RENDER RESOLVED LABELS, NOT RAW JSON: every captured decision is now stored as the `{value, sourceQuote, sourceFile}` envelope (value = `{framework, version}` for versioned, plain string for single-choice). The "Decisions captured" panel currently dumps raw JSON. Render the resolved chip ('service.framework: Spring Boot 4.0') from `answerSummary`. Sweep other sites: the "decision captured" turn (`ConversationMainPane` renders `String(turn.answerValue)`) and "Preview prompt-ready output". This canonical resolved-label text is the format Spec 3 round-trips.
7. FIX THE DOUBLED build.tool ENVELOPE: `{framework:'Maven 3.9', version:'Maven 3.9'}` → `{framework:'Maven', version:'3.9'}` → chip 'Maven 3.9'.
8. CASCADE RE-KEY (main risk, verify first): the laden strings are also cascade `valueByTriggerValue` trigger keys AND recommended-default-map keys. Re-key to stems and confirm the cascade still fires off the stem; verify how the cascade trigger key is derived for a versioned answer (`decisionCaptureOrchestrator.ts` `computeCascadeProposals`) before committing — this gates the change.

CONSTRAINTS: frontend whole-repo tsc/lint baseline is pre-existingly RED (verify in isolation); gateway tests use Jest, frontend uses Vitest; keep the existing capture envelope + POST `/capture` path unchanged (no new AMS DTO); add a guard-rail contract test (every single-choice value ∈ that code's `questionLibrary.choices`, and every versioned stem has a curated default).

OUT OF SCOPE (separate specs): Spec 2 = manifest auto-answer (witness registry, property/plugin extractors, driver→engine & language→runtime inference, one LLM gap-fill, Tier-2 facts); Spec 3 = decisions-file import / template-completion (round-trips this spec's resolved-label text; mutual-exclusivity with the manifest upload box). This spec only makes the answer FORMAT correct.

OPEN QUESTIONS FOR SHAPING: (1) exact flip set for #3 + version-less stems in mixed codes; (2) refresh stale defaults beyond Spring Boot?; (3) auto-select toggle persistence scope + placement; (4) cascade trigger-key derivation for versioned answers (the gating verification); (5) resolved-chip text for a versioned code whose stem is genuinely version-less.
