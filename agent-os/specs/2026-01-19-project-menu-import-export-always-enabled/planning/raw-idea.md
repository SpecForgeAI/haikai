# Raw Idea

## Title
Project Menu: Import/Export Items Must Always Be Enabled and Clickable

## Intent
Ensure the Project menu's file-based actions are always available to the user, including immediately on app startup before any project is loaded or named. Import/Export actions must never be disabled due to "no active project" state or includeDatabase/includeDelivery toggles.
