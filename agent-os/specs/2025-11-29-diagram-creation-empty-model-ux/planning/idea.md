# Raw Idea

Improve the Diagram view for empty models:
1. Add a "New Diagram" name input + [+ New] button in the top toolbar so users can create the first diagram
2. Block palette additions when no diagram exists, showing a clear warning message "Add a new diagram before trying to add items."

Currently when no diagrams exist, clicking palette items does nothing with no feedback. This spec adds proper controls and messaging.
