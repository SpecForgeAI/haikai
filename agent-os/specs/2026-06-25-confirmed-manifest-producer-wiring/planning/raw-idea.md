Feature: Wire the Spec 5 confirmed-manifest producer so a confirmed target dependency manifest (pom.xml / package.json) actually flows VERBATIM into the generated target codebase — replacing the v1 no-op stub. This is "Spec 5 Phase 2", a follow-up to 2026-06-24-confirmed-manifest-to-target-codebase.

WHAT ALREADY EXISTS (do NOT rebuild — the consumer side is 100% done + tested):
- DI seam + null-safe contract `SeedBuildFilesSource = (input:{projectId,bookOfWorkId,targetArchitectureId?}) => Promise<ConfirmedManifestBundle|null>` in gateway/src/services/migrationSeedBuildFilesEnrichment.ts. The honest v1 default `defaultProductionSeedBuildFilesSource` (~line 156) just returns null.
- Handler call site fully wired: gateway/src/services/migrationShapeSpecGenerationHandler.ts:2067 calls resolveSeedBuildFilesEnrichment(deps.seedBuildFilesSource, {projectId, bookOfWorkId, targetArchitectureId}); targetArchitectureId threaded from the book-of-work DTO (handler:859). Production dep wired at gateway/src/routes/migrationShapeSpecGeneration.ts:93.
- Verbatim write-block builder (seedBuildFileWriteBlock.ts), per-module destination resolver (seedBuildFileDestination.ts; ServiceModuleMapping = Record<tag,{moduleDir?|repoRoot?|dir?}>, monorepo default, unresolved tags degrade gracefully), FIRST-sequencing framing, version-unknown passthrough, and the adapter confirmedArtifactsToSeedBundle(artifacts, mapping, layout) — all done.
- Spec 3 already produces ConfirmedManifestArtifact[] (verbatim content, tag, kind, manifestPath, packageLockContent, resolvedDependencies) via buildConfirmedManifestArtifacts(...) in gateway/src/services/targetManifest/manifestHandoffs.ts; the upload route gateway/src/routes/targetManifestUpload.ts returns them on autoAnswer.confirmedManifests (~line 403).

THE GAP (root cause of the stub): the verbatim confirmed manifest is NEVER PERSISTED. Spec 3 builds confirmedManifests[] and returns it only in the upload HTTP response, then discards it. The auto-answer DECISION rows persist (captured-decisions store) but the manifest BYTES do not — so at spec-gen time (a separate later request) there is nothing durable to read back. Helpful alignment: manifests are uploaded keyed by targetArchitectureId, and the spec-gen batch reads bow.targetArchitectureId — SAME key.

KNOWN scope — 4 pieces:
1. PERSIST the confirmed manifest durably, keyed by (projectId, targetArchitectureId, tag). Convention-matching home = AMS: new table (e.g. target_manifest_artifacts) + Liquibase changeset (~199) + write endpoint (at upload/confirm) + read endpoint; append-only / newest-per-tag-wins (mirrors the re-upload-supersede convention). Columns: verbatim content, packageLockContent, kind, manifestPath, tag, resolvedDependencies(json).
2. The "CONFIRMED" TRIGGER. Spec 3 has no explicit confirm step today (upload == auto-answer). Locked design D7 said confirmed = target-state conversation CLOSE (same close that writes target-tech-stack-<id>.md). Decide: persist-at-upload-as-staged + promote-at-close, vs simplest v1 = persist at upload, latest = confirmed.
3. MINT the dedicated seed-build-files STORY. The enrichment only attaches to a book-of-work story with kind='seed_build_files' (isSeedBuildFilesStory), but NOTHING creates that story today (whole-repo grep: only recognition + tests). On confirmation, mint one work item kind='seed_build_files' sequenced FIRST. The existing add-item route already accepts a `kind` field, so the mechanism exists — only the automated trigger is missing.
4. REAL producer + mapping + one-line flip: implement a SeedBuildFilesSource that reads the persisted artifacts for (projectId, targetArchitectureId), supplies a ServiceModuleMapping (tag→module dir) + layout, returns confirmedArtifactsToSeedBundle(...) or null; swap it into productionDeps.seedBuildFilesSource (migrationShapeSpecGeneration.ts:93). Mapping v1 default = monorepo, tag→`<tag>/`; richer mapping later from the target-architecture model.

OPEN QUESTIONS for shaping:
1. Persistence home: new AMS table+endpoints (recommended) vs reuse an existing store; confirm append-only + newest-per-tag read + exact column set.
2. Confirm-trigger timing: at upload (simplest) vs target-state conversation close (matches D7); who calls the persist.
3. Seed-story minting: when (at confirm), by what path (reuse add-item with kind='seed_build_files'?), sequencing (force first), and idempotency on re-confirm/re-upload (replace prior seed story vs append).
4. Service→module MAPPING source for v1: convention (tag→dir) vs read the target-architecture model; layout source (monorepo default).
5. Multiple manifests / re-upload supersede: confirm newest-per-tag wins end-to-end (store + producer).
6. End-to-end verification needs a live env (AMS write/read round-trip + a real spec-gen batch) — none of this runs offline.

CONSTRAINTS: AMS snake_case wire default (@CamelCaseWire only for camelCase consumers); v1 boundary is ZERO IVS change (no new IVS endpoint, no "seed files" upload input — D8 deferred); verbatim bytes survive byte-for-byte; non-blocking / fail-soft (a confirmed-manifest read hiccup degrades to a safe no-op, never breaks the batch); frontend whole-repo baseline is pre-existingly RED — verify in isolation.
