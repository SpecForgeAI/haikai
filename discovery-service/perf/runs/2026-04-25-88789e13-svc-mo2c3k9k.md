# Performance Score — Run 88789e13...

Scored at: 2026-04-25T19:39:18.979Z  ·  Rubric v1  ·  Confidence: no-baseline

**Overall: 2.3 / 5.0**

## Run identification

- **runId:** `88789e13-acac-4733-b710-120f0aa58ab4`
- **service:** svc-mo2c3k9k
- **pack combo:** python-lang / flask
- **weight set:** backend-service
- **mode:** A, tier A

## Golden-anchor comparison

- No prior golden for this pack; this run becomes the seed golden if scoring succeeds.

## Deterministic flags

- **[warn] over-relying-on-llm** — Adapter share is 0% (target 50-70% for annotation-rich packs). Either the language pack is failing to extract IR (parser issue) or the framework pack predicate is not matching (techHints mismatch).
- **[warn] pure-llm-run** — 100% LLM-emitted candidates, 0% adapter. Either the language pack didn't fire (Tier C) or the framework pack predicate didn't match.

## Per-type scores

| Type | Count | Coverage | Accuracy | Metadata | Provenance | Score | Reasoning |
|---|--:|--:|--:|--:|--:|--:|---|
| `interfaces` | 1 | 1 | 2 | 0 | 0 | **0.8** | A Flask service typically should expose more than a single interface-like artifact, so coverage looks thin. The lone row only contains the name `bp` with none of the required interface metadata, and the type is entirely LLM-produced despite the deterministic flags indicating pack failure. |
| `endpoints` | 35 | 4 | 4 | 4 | 0 | **3.0** | The endpoint surface looks plausible for a medium Flask app, and sampled routes such as `/users`, `/login`, and `/tokens` are credible Flask-style resources. Metadata is mostly adequate because method and path are present, but provenance is poor because all endpoints were inferred by the LLM with zero adapter support. |
| `logical_data_entities` | 9 | 3 | 3 | 4 | 2 | **3.0** | The entity list is plausible but skewed toward WTForms-style form classes rather than a broader domain picture, so coverage is only moderate. Required metadata is mostly present via the entity name/class identifier, but this type is also entirely LLM-derived. |
| `logical_data_attributes` | 20 | 3 | 4 | 4 | 2 | **3.3** | The sampled attributes align well with the form-centric logical entities and appear mostly real, including fields like `username`, `password`, and `q`. Coverage is acceptable rather than strong because the model appears to focus on forms only, and provenance remains fully LLM-based. |
| `physical_data_entities` | 5 | 4 | 4 | 4 | 1 | **3.3** | Five persistence entities is a believable count for a Flask microblog-style application, and names like `User`, `Post`, `Message`, and `Notification` look grounded. Metadata clears the minimum bar, but the absence of any deterministic extraction on a backend type that should often have some parser help lowers provenance confidence. |
| `physical_data_attributes` | 29 | 4 | 4 | 1 | 1 | **2.5** | Attribute coverage appears healthy and the sampled fields are believable for ORM models. However, required metadata is very sparse because rows generally lack `columnName` and other structural fields expected for this type, and all rows are LLM-emitted. |
| `logical_data_entity_relationships` | 6 | 4 | 4 | 5 | 2 | **3.8** | The relationships look coherent for the sampled entities, including sensible user-to-post/message/notification links and a plausible self-referential user follow graph. Required metadata is present and cardinality is supplied as useful context, but provenance is still entirely LLM-side. |
| `interface_logical_entities` | 0 | 0 | 0 | 0 | 0 | **0.0** | No interface-to-logical-entity links were produced, which is a noticeable gap given the presence of many endpoints and form/domain classes. This missing connective surface weakens traceability from API/interface layer to data concepts. |
| `business_logics` | 40 | 4 | 3 | 3 | 0 | **2.5** | There is substantial business-logic coverage, and several sampled methods such as `get_token`, `revoke_token`, and validation helpers seem legitimate. Accuracy is mixed because some entries blur route handlers, properties, and methods, metadata is minimal, and the deterministic flags make the 100% LLM provenance a significant concern. |
| `ui_screens` | 0 | 0 | 0 | 0 | 0 | **0.0** | No UI screens were emitted. This type is not weighted for the backend-service profile, so it does not affect the overall score. |
| `ui_components` | 0 | 0 | 0 | 0 | 0 | **0.0** | No UI components were emitted. This type is not weighted for the backend-service profile, so it does not affect the overall score. |

## Cross-cutting axes

| Axis | Score |
|---|--:|
| Determinism | 0 |
| Hallucination rate | 3 |
| Coverage of obvious gaps | 2 |
| Cost | 3 |

## Anomalies

- Run is a pure-LLM extraction: 145 of 145 candidates are LLM-emitted and adapter share is 0%, directly matching both deterministic warning flags.
- Endpoint and data-model surfaces look broadly plausible, which prevents this from being a total failure, but confidence is limited because none of the surfaces are grounded by deterministic extraction.
- Interface coverage is very weak, with a single ambiguous `bp` row lacking required metadata.
- Interface-to-logical-entity linkage is completely absent despite nontrivial endpoint and entity counts.
- Physical data attributes are structurally thin: sampled rows usually omit required database-oriented metadata such as `columnName`.
- No baseline exists for python-lang/flask yet, so coverage judgments are absolute rather than pack-anchored.

---

## Raw score JSON

```json
{
  "runId": "88789e13-acac-4733-b710-120f0aa58ab4",
  "scoredAt": "2026-04-25T19:39:18.979Z",
  "rubricVersion": "1",
  "packCombo": {
    "language": "python-lang",
    "frameworks": [
      "flask"
    ]
  },
  "weightSet": "backend-service",
  "goldenAnchor": {
    "runId": null,
    "score": null
  },
  "deterministicFlags": [
    {
      "code": "over-relying-on-llm",
      "severity": "warn",
      "message": "Adapter share is 0% (target 50-70% for annotation-rich packs). Either the language pack is failing to extract IR (parser issue) or the framework pack predicate is not matching (techHints mismatch)."
    },
    {
      "code": "pure-llm-run",
      "severity": "warn",
      "message": "100% LLM-emitted candidates, 0% adapter. Either the language pack didn't fire (Tier C) or the framework pack predicate didn't match."
    }
  ],
  "perType": {
    "interfaces": {
      "count": 1,
      "axes": {
        "coverage": 1,
        "accuracy": 2,
        "metadataRichness": 0,
        "provenanceBalance": 0
      },
      "score": 0.8,
      "reasoning": "A Flask service typically should expose more than a single interface-like artifact, so coverage looks thin. The lone row only contains the name `bp` with none of the required interface metadata, and the type is entirely LLM-produced despite the deterministic flags indicating pack failure."
    },
    "endpoints": {
      "count": 35,
      "axes": {
        "coverage": 4,
        "accuracy": 4,
        "metadataRichness": 4,
        "provenanceBalance": 0
      },
      "score": 3,
      "reasoning": "The endpoint surface looks plausible for a medium Flask app, and sampled routes such as `/users`, `/login`, and `/tokens` are credible Flask-style resources. Metadata is mostly adequate because method and path are present, but provenance is poor because all endpoints were inferred by the LLM with zero adapter support."
    },
    "logical_data_entities": {
      "count": 9,
      "axes": {
        "coverage": 3,
        "accuracy": 3,
        "metadataRichness": 4,
        "provenanceBalance": 2
      },
      "score": 3,
      "reasoning": "The entity list is plausible but skewed toward WTForms-style form classes rather than a broader domain picture, so coverage is only moderate. Required metadata is mostly present via the entity name/class identifier, but this type is also entirely LLM-derived."
    },
    "logical_data_attributes": {
      "count": 20,
      "axes": {
        "coverage": 3,
        "accuracy": 4,
        "metadataRichness": 4,
        "provenanceBalance": 2
      },
      "score": 3.3,
      "reasoning": "The sampled attributes align well with the form-centric logical entities and appear mostly real, including fields like `username`, `password`, and `q`. Coverage is acceptable rather than strong because the model appears to focus on forms only, and provenance remains fully LLM-based."
    },
    "physical_data_entities": {
      "count": 5,
      "axes": {
        "coverage": 4,
        "accuracy": 4,
        "metadataRichness": 4,
        "provenanceBalance": 1
      },
      "score": 3.3,
      "reasoning": "Five persistence entities is a believable count for a Flask microblog-style application, and names like `User`, `Post`, `Message`, and `Notification` look grounded. Metadata clears the minimum bar, but the absence of any deterministic extraction on a backend type that should often have some parser help lowers provenance confidence."
    },
    "physical_data_attributes": {
      "count": 29,
      "axes": {
        "coverage": 4,
        "accuracy": 4,
        "metadataRichness": 1,
        "provenanceBalance": 1
      },
      "score": 2.5,
      "reasoning": "Attribute coverage appears healthy and the sampled fields are believable for ORM models. However, required metadata is very sparse because rows generally lack `columnName` and other structural fields expected for this type, and all rows are LLM-emitted."
    },
    "logical_data_entity_relationships": {
      "count": 6,
      "axes": {
        "coverage": 4,
        "accuracy": 4,
        "metadataRichness": 5,
        "provenanceBalance": 2
      },
      "score": 3.8,
      "reasoning": "The relationships look coherent for the sampled entities, including sensible user-to-post/message/notification links and a plausible self-referential user follow graph. Required metadata is present and cardinality is supplied as useful context, but provenance is still entirely LLM-side."
    },
    "interface_logical_entities": {
      "count": 0,
      "axes": {
        "coverage": 0,
        "accuracy": 0,
        "metadataRichness": 0,
        "provenanceBalance": 0
      },
      "score": 0,
      "reasoning": "No interface-to-logical-entity links were produced, which is a noticeable gap given the presence of many endpoints and form/domain classes. This missing connective surface weakens traceability from API/interface layer to data concepts."
    },
    "business_logics": {
      "count": 40,
      "axes": {
        "coverage": 4,
        "accuracy": 3,
        "metadataRichness": 3,
        "provenanceBalance": 0
      },
      "score": 2.5,
      "reasoning": "There is substantial business-logic coverage, and several sampled methods such as `get_token`, `revoke_token`, and validation helpers seem legitimate. Accuracy is mixed because some entries blur route handlers, properties, and methods, metadata is minimal, and the deterministic flags make the 100% LLM provenance a significant concern."
    },
    "ui_screens": {
      "count": 0,
      "axes": {
        "coverage": 0,
        "accuracy": 0,
        "metadataRichness": 0,
        "provenanceBalance": 0
      },
      "score": 0,
      "reasoning": "No UI screens were emitted. This type is not weighted for the backend-service profile, so it does not affect the overall score."
    },
    "ui_components": {
      "count": 0,
      "axes": {
        "coverage": 0,
        "accuracy": 0,
        "metadataRichness": 0,
        "provenanceBalance": 0
      },
      "score": 0,
      "reasoning": "No UI components were emitted. This type is not weighted for the backend-service profile, so it does not affect the overall score."
    }
  },
  "crossCutting": {
    "determinism": 0,
    "hallucinationRate": 3,
    "coverageOfObviousGaps": 2,
    "cost": 3
  },
  "overall": 2.3,
  "confidence": "no-baseline",
  "anomalies": [
    "Run is a pure-LLM extraction: 145 of 145 candidates are LLM-emitted and adapter share is 0%, directly matching both deterministic warning flags.",
    "Endpoint and data-model surfaces look broadly plausible, which prevents this from being a total failure, but confidence is limited because none of the surfaces are grounded by deterministic extraction.",
    "Interface coverage is very weak, with a single ambiguous `bp` row lacking required metadata.",
    "Interface-to-logical-entity linkage is completely absent despite nontrivial endpoint and entity counts.",
    "Physical data attributes are structurally thin: sampled rows usually omit required database-oriented metadata such as `columnName`.",
    "No baseline exists for python-lang/flask yet, so coverage judgments are absolute rather than pack-anchored."
  ]
}
```
