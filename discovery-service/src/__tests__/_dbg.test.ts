// Scratch file (could not be deleted in this environment). Intentionally a
// single trivial passing test so it does not break the jest suite.
test('placeholder', () => {
  expect(true).toBe(true);
});
